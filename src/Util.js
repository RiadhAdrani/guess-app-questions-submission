const checkSubmission = (question, answers) => {
     if (question.length < 3) {
          return false;
     }

     if (!question) {
          return false;
     }

     answers;
};

export default checkSubmission;
