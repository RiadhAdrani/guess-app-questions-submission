class Answer {
     constructor({ answer = "", points = 1 }) {
          this.answer = answer;
          this.points = points;
     }

     static pointsList = [-5, 1, 2, 5];
}

export default Answer;
